

def generate_price_graph(obj, terms, start_date, end_date, graph = False, blob = False):
	"""
	inputs
	obj: Crypto class object
	terms: list(strings) = ['term1', 'term2', ... ]
	start_date, end_date: strings = YYYY-MM-DD
	graph, blob: bool
	
	output
	if Graph is on, then it displays a graph.
	if Blob is on, then it returns the graph as a base 64 string, to be put into an html file
	"""

	

	pass